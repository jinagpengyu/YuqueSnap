# ============ Node / pnpm ============
node_modules/
# pnpm 会生成的调试日志
pnpm-debug.log*
.pnpm-debug.log*

# 如果启用了 Plug'n'Play（一般不用）
.pnp.*
.pnp.cjs

# ============ TypeScript 构建输出 ============
dist/
out/
build/
release/

# TS 增量编译信息
*.tsbuildinfo

# ============ Electron 相关 ============
*.asar
*.asar.unpacked/

# 如果你用 electron-builder / forge 生成安装包，可忽略产物目录（按实际调整）
/installer/
*.dmg
*.exe
*.AppImage
*.snap
*.msi
*.blockmap
latest.yml
builder-debug.yml
builder-effective-config.yaml

# ============ 日志与调试文件 ============
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
debug.log

# 崩溃/诊断
*.stackdump
*.pid
*.pid.lock

# ============ 测试与覆盖率 ============
coverage/
.nyc_output/
*.lcov

# Jest / Vitest / 其他测试临时缓存（如后续使用）
.vitest/
jest-cache/
jest-temp/

# ============ 缓存 ============
.cache/
.eslintcache
.stylelintcache
parcel-cache/
turbo/
.rpt2_cache/
.rollup.cache/
vite.config.timestamp-*

# ============ 环境配置 ============
.env
.env.*
!.env.example

# ============ 编辑器 & IDE ============
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo

# ============ OS / 杂项 ============
.DS_Store
Thumbs.db
*.tmp
*.temp
*.bak
*.orig

# ============ 截图 / 临时生成文件（按你项目实际目录命名） ============
temp/
tmp/
temps/
screenshots/
captures/
capture-temp/
*.png~
*.png.tmp

# 如果你在开发中把生成的截图放进某固定目录（例如 assets/raw/），可取消注释： 
# assets/raw/

# ============ 打包 / 分发临时目录（按需启用） ============
# dist-electron/
# publish/

# ============ 依赖分析输出 / 统计 ============
stats.json

# ============ 其它可能的工具文件 ============
*.orig.*      # merge 冲突残留
*.rej         # patch 拒绝文件

# ============ 不忽略的文件（白名单示例） ============
# 确保锁定依赖版本 —— 建议保留 pnpm-lock.yaml
!pnpm-lock.yaml